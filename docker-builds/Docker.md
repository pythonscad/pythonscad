# Docker

This directory contain Dockerfiles which can build PythonSCAD inside Docker containers based on various Linux distributions and versions.
It should serve as an easy and lightweight testbed for local builds on different OS versions.

To build an image change to that subdirectory and run `docker build .`.
