; Unregister file associations (SHCTX = HKLM or HKCU based on SetShellVarContext set in un.MultiUser.OnInit)
${UnRegisterExtension} ".scad" "PythonSCAD_File"
${UnRegisterExtension} ".scad" "OpenSCAD_File"
${UnRegisterExtension} ".py" "PythonSCAD_Python_File"

; Remove start menu shortcuts from the appropriate scope
Delete "$SMPROGRAMS\PythonSCAD\PythonSCAD.lnk"
Delete "$SMPROGRAMS\PythonSCAD\Uninstall PythonSCAD.lnk"
RMDir "$SMPROGRAMS\PythonSCAD"
Delete "$SMPROGRAMS\PythonSCAD.lnk"

; Remove install mode registry key
DeleteRegKey HKCU "Software\PythonSCAD"
${If} $MultiUser.InstallMode = 1
  DeleteRegKey HKLM "Software\PythonSCAD"
${EndIf}

${RefreshShellIcons}
